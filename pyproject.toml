[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tsrlm"
version = "0.1.0"
description = "Time-series report language model skeleton (PatchTST encoder + prefix bridge + HF causal LM)"
requires-python = ">=3.9"
dependencies = []
